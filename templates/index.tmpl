{{template "layout"}}

{{define "title"}}
<title>Home</title>
{{end}}

{{define "content"}}
<div class="container m-3">
    <h1>Videos</h1>
    <ul>
        {{range .Videos}}
        <li><a href="/watch/{{.ID}}">{{.Title}}</a></li>
        {{end}}
    </ul>
</div>
{{end}}